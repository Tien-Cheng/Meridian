### S0-2: Verify Schema Deploy and Seed
- **Tier:** Setup (prerequisite)
- **Est. time:** 5 min
- **Depends on:** S0-1
- **Blocks:** All tasks
- **Owner:** Builder B
- **Files:** `convex/schema.ts`, `setup.mjs`
- **Output contract:** Convex backend fully deployed with all 7 tables + auth tables visible in dashboard
- **Acceptance criteria:**
  - `npx convex dev` succeeds without schema validation errors
  - All tables visible in Convex dashboard: `investigations`, `findings`, `agentMonitor`, `sellerDossiers`, `supplyRoutes`, `cases`, plus auth tables
  - `setup.mjs` runs without errors (seeds initial data if applicable)
- **Gotchas:**
  - If workflow component not installed: `npx convex component add @convex-dev/workflow`
  - If agent component not installed: `npx convex component add @convex-dev/agent`
  - Schema is already complete in `convex/schema.ts` — do NOT modify it
  - Check `convex/convex.config.ts` references both `@convex-dev/agent` and `@convex-dev/workflow`
