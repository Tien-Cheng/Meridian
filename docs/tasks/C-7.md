### C-7: sellerDossiers Query Function
- **Tier:** 2
- **Est. time:** 15 min
- **Depends on:** C-2
- **Blocks:** A-7
- **Files:** New file `convex/functions/sellerDossiers.ts`
- **Output contract:**
  1. `listByInvestigation` query — returns all seller dossiers for an investigation
  2. `create` internal mutation — inserts a seller dossier matching the schema
- **Acceptance criteria:**
  - Query uses `by_investigation` index on `sellerDossiers` table
  - Returns full dossier objects with all schema fields
  - Create mutation validates all required fields match `convex/schema.ts`
- **Gotchas:**
  - This is straightforward — follows the exact same pattern as `convex/functions/findings.ts`
  - The `create` mutation may already be needed by C-2 — coordinate with C-2 to avoid duplication
  - Export the query via the public API so the frontend `EvidencePanel` can subscribe to it
  - The `relatedListingIds` field is `v.array(v.id("findings"))` — the query should return these as-is (frontend can use them to cross-reference findings)
